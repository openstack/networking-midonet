-e git://git.openstack.org/openstack/neutron.git@stable/icehouse#egg=neutron

# Some components that the Icehouse plugin depends on are not compatible with
# oslo.i18n 2.0.0 which is now available.  Until the library requirements are
# decided for the stable releases, pin these to the version matching the
# version defined in Icehouse's OpenStack global requirements.
oslo.i18n>=0.1.0,<1.1  # Apache-2.0
